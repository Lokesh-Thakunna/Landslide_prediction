# BHURAKSHAN ML and Data

Runnable ML and data workspace for:

- proxy feature generation
- Random Forest training
- current plus `+1h` and `+2h` scoring
- internal FastAPI prediction endpoint
- sample persistence outputs for prediction snapshots
